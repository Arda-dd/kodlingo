name: kodlingo
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Temiz mimari ve State Management için eklenen paketler
  http: ^1.2.1 # API çağrıları için
  shared_preferences: ^2.2.3 # Yerel veri depolama için
  provider: ^6.1.2 # UserNotifier (ChangeNotifier) yönetimi için
  flutter_riverpod: ^2.5.1 # LessonNotifier (StateNotifier) yönetimi için

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true